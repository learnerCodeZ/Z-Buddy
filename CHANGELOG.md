# Changelog

本文件为 Z-Buddy 的用户视角更新日志（Keep a Changelog 风格）；
开发视角的过程记录见各阶段总结与提交历史。

## [Unreleased]

### 新增

- **默认桌宠「夜羽」（`yoru`）**：插画形象（黑裙天使 + 头顶睡意 Z）。四态动画（呼吸浮动 / 蹦跳摆头 / 出错抖动 / 安睡呼吸）由新工具
  `app/tools/gen_illustration_pet.mjs` 从一张完整立绘自动生成图集：白底泛洪抠图 + 白底反解羽化（深色桌面上不留白边）、
  头顶装饰自动分离（Z 只出现在睡觉行）、4× 超采样渲染后降采样；
- 桌宠窗精灵画布 128 → **192 px**：与夜羽的 192 帧 1:1 绘制（零缩放最清晰），既有 64 帧像素宠物随之整数 3 倍放大，依旧锐利；
- 内置宠物列表新增 `yoru`（排在首位）：宠物管理页、托盘「切换宠物」、加载失败回退均可选到；
- **长按拖动 = 换成拖动形象**：按住桌宠 220ms 即"被拎起来"，形象切换成黑天鹅鸭子（单张立绘，**静态无动效**），
  左右两个朝向各一张（其中一张是另一张的水平镜像）—— 向左（含左上/左下）与正上方用 `drag_left`，
  其余（含向右、正下方）用 `drag_right`；单击暂停、右键菜单、普通拖动挪窗口全部不受影响（长按后松手不会误触发暂停）；
- 精灵图行数/列数**不再固定 4×4**：`pet.json` 的 `states` 是唯一契约，本次夜羽升到 **6 行**
  （基础六态各 6 帧 + drag_left / drag_right 各 1 帧），不含拖动形象键的宠物包自动降级；
- 生成器新增 `--drag <立绘.png>`：自动抠白底（阈值 250）、裁切到内容包围盒、写入 row 4/5 并生成水平镜像
  （`--drag-faces left` 可声明立绘朝向）；PNG 编码改为逐行自适应过滤（图集体积下降）。

### 修复

- 拖动形象左向（镜像）那一份的**字形是反的**：新增 `--drag-upright`，把头顶的 Z、Z 的卷尾、
  胸针心形里的 Z 在镜像后按原方向贴回 —— 现在鸭子是镜像的、字是正的
  （校验：左向帧的字形与"正字参考"逐像素一致；两帧与纯镜像的差异仅 464px / 36864px，即被故意翻正的字形）。

### 变更

- 默认宠物从团子 `mochi` 改为夜羽 `yoru`；已在 `~/.z-buddy/app.json` 存过偏好的用户不受影响（仍用自己选的宠物）；
- 夜羽各状态行由 4 帧升为 6 帧（动作更顺），`fps` 相应重算以保持每轮循环时长不变；
- 新增 Rust 命令 `cursor_pos`：原生拖动窗口时 WebView 收不到 mousemove（Windows 系统模态移动循环），
  因此拖动形象由前端每 80ms 轮询光标位置决定用哪一侧。

### 待办

- 官网宠物画廊尚未上架夜羽（需要 webp 循环图，`gen_site_assets.py` 依赖 Pillow）。

## [0.1.0] - 2026-09-06

### 新增

- **状态联动**：六态动画（idle / thinking / working / 等待审批 / 出错 / 深夜睡觉）实时映射 ZCode Agent 生命周期；
- **点按暂停**：点击桌宠即暂停 ZCode（hook 硬拦截，非装饰按钮），再点恢复；命令文本含 `.z-buddy/pause` 路径可豁免；
- **悬浮窗**：置顶无边框、点按穿透、自由拖动、多显示器与 DPI 适配；
- **系统托盘**：显示/隐藏、切换宠物、退出；
- **宠物包加载器**：`~/.z-buddy/pets/` 外部宠物包扫描、托盘热切换、偏好持久化（`app.json`）；
- **内置原创宠物**：团子 Mochi、抹茶 Matcha（PIL 程序化生成）；
- **发行产物**：NSIS 安装包（中英双语向导、自选路径、可选开机自启）+ 便携版，约 4.2 MB；
- **官网**：首页 / 下载 / 快速开始 / FAQ / 宠物画廊 / 更新日志（Astro + Tailwind CSS v4，部署于 GitHub Pages）。

### 已知限制

- 多 ZCode 会话并发时状态为「最后写入者胜」，会话聚合排期 Phase 2；
- 未购买代码签名证书，SmartScreen 会提示放行（见官网下载页引导）；
- macOS / Linux 支持：Tauri 跨平台可行，排期待定。

[0.1.0]: https://github.com/learnerCodeZ/Z-Buddy/releases/tag/v0.1.0
